/**
 * 버튼 모서리 둥글기 — 완전 원형(pill)이 아닌 직사각형 라운드
 * 디자인 시안에 맞게 이 값만 조정하면 전체 버튼에 반영됩니다.
 */

export const Radii = {
  /** 큰·중간·작은 primary 버튼 (높이 40~60) */
  button: 12,
  /** 태그 버튼 (높이 40) */
  tag: 8,
} as const;
